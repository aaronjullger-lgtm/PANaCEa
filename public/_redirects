# Cloudflare Pages serves Vite assets directly and provides built-in SPA
# fallback routing when no top-level 404.html exists. Avoid self-rewrite rules
# such as `/* /index.html 200`; Wrangler rejects them as infinite loops.
