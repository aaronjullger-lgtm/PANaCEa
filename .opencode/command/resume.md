---
description: Resume from the latest handoff note. Reconstruct working context after a session gap.
agent: orchestrator
---

Resume from the latest handoff note.

1. Read `.opencode/handoff.md` — what was done, blockers, next steps
2. Run `git status` and `git log --oneline -5` — current state
3. Read `.opencode/knowledge/learnings.md` — relevant patterns
4. Summarize: where we are, what's next, the exact next command

Follow the `session-resume` skill if available.

$ARGUMENTS
