# src/components – Legacy / Minimal

React components have been **moved to the root `components/`** directory (canonical location).

- **Use:** `@/components/...` for all component imports.
- **Do not add** new components here; add them under root `components/`.
- See `.cursor/rules/project-conventions.mdc` and `docs/SCRIPTS_REFERENCE.md`.

This folder may contain re-exports or remain empty; the single source of truth is `components/`.
