# PANaCEa Next Work Discovery

Status: continuation discovery pass refreshed 2026-05-05 11:36 EDT.

| Finding | Severity | Area | Evidence | Why It Matters | Recommended Fix | Verification |
|---|---|---|---|---|---|---|
| Direct canonical `Question` submissions were not filtered by production lifecycle in legacy attempt endpoint | Addressed P1 | Attempt/scoring pipeline | `functions/api/questions/attempt.ts` now uses `findFirst(withProductionQuestionSafety({ id }))` | A draft/unreviewed canonical question ID could have been written to `QuestionAttempt` by compatibility callers | Keep regression coverage and continue broader identity migration | `npx vitest run functions/api/questions/attempt.test.ts` |
| Direct canonical `Question` records were not filtered by production lifecycle in legacy record endpoint | Addressed P1 | Attempt/scoring pipeline | `functions/api/questions/record.ts` now uses `findFirst(withProductionQuestionSafety({ id }))` and `UserQuestionSeen` uses resolved ID | Seen-history/stat writes should not bless unapproved clinical content | Keep regression coverage | `npx vitest run functions/api/questions/record.test.ts` |
| Due-sibling variant seeding could inspect original canonical questions without the shared production predicate | Addressed P2 | Review generation | `functions/api/questions/due-siblings.ts` now uses production filters for pre-generated and canonical originals | Variant prompts should not be seeded from draft/rejected rows | Keep focused regression coverage | `npx vitest run functions/api/questions/due-siblings.test.ts` |
| Question context endpoint used direct lifecycle-unfiltered `Question` reads and Clerk IDs for `ReviewLog` lookup | Addressed P1 | Context/explanation pipeline | `functions/api/questions/context.ts` now uses `withProductionQuestionSafety` and resolves `auth.userId` to internal `User.id`; `functions/api/questions/context.test.ts` covers both behaviors | Learner context should not expose draft/unapproved question metadata, and related-performance summaries should use the correct user identity domain | Keep regression coverage and inspect explanation generation/gateway consistency next | `npx vitest run functions/api/questions/context.test.ts` |
| Canonical source identity still needs migration/backfill | P0 | Data integrity | `FINAL_PRODUCTION_READINESS_REPORT.md`, `DATA_PIPELINE_AND_SCHEDULING_AUDIT.md`, `StudySession.questionIds String[]` and split `Question`/`PreGeneratedQuestion` roots in `prisma/schema.prisma` | Historical attempts/sessions/reviews cannot be proven end-to-end without source identity | Run read-only probe on disposable/prod-like DB; design migration/backfill | Audit script output plus migration plan |
| `UserProgress.conditionId` identity remains split | P0 | FSRS/progress | `DATA_PIPELINE_AND_SCHEDULING_AUDIT.md`, `prisma/schema.prisma` | Progress rows can point at the wrong concept domain unless migrated | Keep guard fail-closed; design explicit `medicalContentId`/concept identity migration | FK/domain guard tests and DB probe |
| StudyPlanTask V2 is partially consolidated | P1 | Study plan/scheduling | `functions/api/_shared/studyPlanService.ts`, `lib/services/studyPlanService.ts`, `/api/users/me/daily-plan` compatibility | Personalized plans need one launch/completion/source-of-truth contract | Continue normalizing task shape while preserving route compatibility | Study-plan service and route tests |
| Targeted study-plan tasks could still carry hidden `rapid_recall` task mode | Addressed P1 | Study plan/scheduling | `lib/services/studyPlanService.ts` now emits/canonicalizes task mode `targeted` and routes condition scope separately | Hidden/deferred mode IDs should not leak into active daily plans | Keep route-scope tests and continue full V2 consolidation | `npx vitest run lib/services/studyPlanService.test.ts hooks/useStudyPlanLaunch.test.tsx` |
| Persisted current-plan task output could carry stale extra `mode` and launch settings | Addressed P1 | Study plan/scheduling | `functions/api/_shared/studyPlanService.ts` now normalizes targeted/review task `mode` and `launchSettings` before current-plan output | Current plan consumers should not see deferred mode names as active work | Continue migration-backed cleanup of stored `recommendedModes` arrays | `npx vitest run functions/api/_shared/studyPlanService.test.ts` |
| Single-system current-plan tasks launched as generic adaptive sessions | Addressed P2 | Study plan/scheduling | `functions/api/_shared/studyPlanService.ts` now emits `mode=system` when a plan task has exactly one launch system and no condition scope; coverage lives in `functions/api/_shared/studyPlanService.test.ts` | A visible plan CTA for a single system should not silently become broad adaptive work | Keep route-registry/launch tests and continue V2 task contract consolidation | `npx vitest run functions/api/_shared/studyPlanService.test.ts hooks/useStudyPlanLaunch.test.tsx` |
| Generated-question adapter remains fragmented | P1 partial | AI generation | `functions/api/_shared/generated-question-preview.ts` now centralizes preview-only/staging metadata for primary and RAG generation, but `generate-enhanced.ts`, `generate-deep.ts`, batch/refill, and prompt schemas still have distinct contracts | Question generation can be safe in one route and divergent elsewhere | Continue from shared preview/staging helper into canonical generated-question schema and prompt registry | Generation route/schema tests |
| Primary and RAG generation duplicated preview-only/staging metadata code | Addressed P1 | AI generation | `functions/api/questions/generate.ts` and `functions/api/questions/generate-rag.ts` now call `stageGeneratedQuestionPreview`; helper coverage lives in `functions/api/_shared/generated-question-preview.test.ts` | Duplicated metadata code already allowed one route to return unstaged content; a shared helper lowers recurrence risk | Keep helper tests and migrate future generation routes through the helper or a stricter schema adapter | `npx vitest run functions/api/_shared/generated-question-preview.test.ts functions/api/questions/generate.test.ts functions/api/questions/generate-rag.test.ts` |
| Enhanced generation could promote live rows after staging provenance failed | Addressed P1 | AI generation / provenance | `functions/api/questions/generate-enhanced.ts` now throws before `question.create` when `stagingQuestion.create` fails; `functions/api/questions/generate-enhanced.test.ts` proves no live write occurs | A CoVe-passed question without a staging/provenance row is harder to audit, review, or trace back through the content pipeline | Keep route regression and later move enhanced generation into the canonical schema/prompt adapter | `npx vitest run functions/api/questions/generate-enhanced.test.ts` |
| Admin-only deep generation reused failed-staging terminology | Addressed P2 | AI generation / admin preview | `functions/api/questions/generate-deep.ts` now uses `persistence: "admin_preview_only"` and `adminPreviewOnly: true`; `functions/api/questions/generate-deep.test.ts` proves `not_staged` is absent | Admin authoring previews should not look like failed learner-facing staging artifacts | Keep route admin-only; migrate to canonical schema before learner exposure | `npx vitest run functions/api/questions/generate-deep.test.ts` |
| RAG question generation could return unstaged preview questions | Addressed P1 | AI generation | `functions/api/questions/generate-rag.ts` now skips unstaged generated items and returns `ErrorCode.DB_ERROR` 502 when no generated question can be staged; `functions/api/questions/generate-rag.test.ts` covers the fail-closed path | Generated clinical questions should never appear learner-facing unless they have a review/staging identity that can be audited and promoted safely | Keep RAG route regression and centralize generated-question adapters next | `npx vitest run functions/api/questions/generate-rag.test.ts` |
| Primary question generation could return and cache unstaged generated questions | Addressed P1 | AI generation | `functions/api/questions/generate.ts` now returns `ErrorCode.DB_ERROR` 502 when a new generation cannot be staged; `functions/api/questions/generate.test.ts` proves the generated text is not returned or cached | A newly generated clinical question without staging identity should not become a cached preview or learner-facing artifact | Keep route regression and centralize generated-question adapters next | `npx vitest run functions/api/questions/generate.test.ts` |
| Explanation route bypassed the shared AI gateway and returned synthetic fallback prose on provider failure | Addressed P1 | Explanation generation | `functions/api/questions/explain-rag.ts` now uses `gateway.callStructured` with a Zod explanation schema, ignores stale fallback cache entries, and returns explicit unavailable errors on gateway/no-context failure; `functions/api/questions/explain-rag.test.ts` covers gateway success and fail-closed failure | Learners should not receive ungrounded generated-looking fallback explanation text when AI/RAG cannot produce a grounded, validated response | Keep focused route regressions and add consumer/UI handling for unavailable explanation states if needed | `npx vitest run functions/api/questions/explain-rag.test.ts` |
| Structured condition cards endpoint bypassed shared AI gateway with direct Gemini fetch and ad-hoc JSON parsing | Addressed P1 | Medical content / condition page | `functions/api/conditions/[identifier]/structured.ts` now uses `gateway.callStructured` with `StructuredConditionSchema`, and `functions/api/conditions/[identifier]/structured.test.ts` covers gateway success plus fallback to stored fields | Condition pages are an active product surface; extraction should use shared telemetry/schema handling and should not depend on raw provider calls | Keep route regression and continue direct-AI-call census for remaining admin/visualizer/library routes | `npx vitest run 'functions/api/conditions/[identifier]/structured.test.ts'` |
| Library answer endpoint generated user-facing answers through direct Gemini fetch and raw console logging | Addressed P1 | Medical library / answer generation | `functions/api/library/answer.ts` now uses `gateway.callText` for answer generation and `createEndpointLogger`; `functions/api/library/answer.test.ts` covers gateway success and gateway failure with searched references preserved | Medical library answers are an active product surface; generation should share policy, telemetry, and error handling | Keep regression coverage; consolidate embedding calls separately | `npx vitest run functions/api/library/answer.test.ts` |
| Admin condition enrichment used direct Gemini fetch and ad-hoc JSON parsing before writing MedicalContent | Addressed P1 | Admin content enrichment / medical database | `functions/api/admin/enrich-condition.ts` now uses `gateway.callStructured` with `EnrichmentResponseSchema`; `functions/api/admin/enrich-condition.test.ts` covers successful requested-field writes and fail-closed gateway failure | Admin enrichment writes clinical content; model output must be validated and failed generations must not mutate content | Keep admin-only review/provenance gates and continue direct-AI-call cleanup | `npx vitest run functions/api/admin/enrich-condition.test.ts` |
| Embedding calls were duplicated across active library/search/RAG utilities | Addressed P2 | Search/RAG infrastructure | `functions/api/library/answer.ts`, `functions/api/library/semantic-search.ts`, `lib/services/ragContextService.ts`, `lib/services/search/hybridSearch.ts`, and `lib/search.ts` now use shared `lib/gemini.ts#getEmbedding` | Active library/RAG code now shares one embedding error/dimension/timing boundary instead of five local fetch implementations | Keep tests green; consider a first-class gateway embedding method later | `npx vitest run functions/api/library/answer.test.ts lib/services/search/hybridSearch.test.ts tests/ragContextService.test.ts tests/explainRag.test.ts` |
| Direct embedding calls remain in batch/backfill utilities | P3 | Search/RAG infrastructure | `functions/api/embeddings/generate-questions.ts` embeds questions with `gemini-embedding-2` (768-dim) and `lib/evaluation/embeddingBenchmark.ts` calls embeddings directly | Backfill/evaluation paths have different model/version requirements and should not be mixed into active library/RAG routing without a compatibility review | Review separately when question embedding schema/model versioning is tackled | Focused admin embedding tests plus typecheck |
| Live runtime smoke is absent | P1 | Deployment/QA | `FINAL_PRODUCTION_READINESS_REPORT.md` | Local Vitest cannot prove Cloudflare runtime, Clerk auth, DB FKs, redirects, or route gates | Add documented smoke suite gated by `BASE_URL` and test credentials | Wrangler/browser/API smoke |
| Design-token lint warnings remain broad | P2 | UI/UX | `IMPLEMENTATION_LOG.md` reports 422 raw-color/design-token warnings and zero lint errors | UI can regress visually despite passing lint | Treat as a separate UI-token migration, not mixed with data work | Dedicated UI lint baseline and migration |
| Legacy SRS compatibility shells remain | P1 | Deprecated code/FSRS | `DEPRECATED_CODE_AND_CONFLICTS_AUDIT.md`, `/api/srs/*`, `SRSItem` schema | Premature deletion can break clients; indefinite retention can confuse ownership | Keep until browser/runtime compatibility is proven, then remove with migration-backed cleanup | Import census, route smoke, migration plan |
