# PANaCEa Continuation Review

Status: continuation pass refreshed 2026-05-05 11:36 EDT.

## Summary

The previous work materially improved production readiness: validation passes, production dependency audit is clean, the adaptive dashboard is registry-driven, deferred modes are hidden more consistently, Todoist linking is removed, study-plan launch/completion links are stronger, and the active review pipeline is closer to a single canonical path.

The app remains **no-launch** because the remaining risks are not cosmetic. They are mostly identity and truth risks in the learning pipeline: canonical question/source identity, canonical condition/content identity, conservative generation-to-servable policy, partial StudyPlanTask V2 consolidation, and no live Cloudflare/Clerk/Postgres smoke gate.

## Prior Work Review

| Area | Previous Status | Current Status | Evidence | Remaining Risk | Next Action |
|---|---|---|---|---|---|
| Build/test baseline | Reported passing | Re-run for latest slices | Latest continuation checks include targeted Vitest, typecheck, lint, build, and `git diff --check` | Full `npm test` and live runtime smoke were not re-run in this pass | Keep targeted checks per slice; run full suite before handoff/release |
| Production question serving | Improved | Legacy direct canonical attempt/record paths now use production safety filters | `functions/api/questions/attempt.ts`, `functions/api/questions/record.ts`, `functions/api/questions/due-siblings.ts` | Broader canonical source identity still needs migration/backfill | Keep focused regressions and continue identity migration planning |
| Question context endpoint | Newly reviewed | Hardened in this continuation pass | `functions/api/questions/context.ts` now resolves the internal user row, uses `withProductionQuestionSafety`, and avoids direct draft/unapproved `Question` reads; `functions/api/questions/context.test.ts` covers safe and unsafe IDs | Related explanation routes still need gateway/schema consistency review | Keep the focused context regression and inspect explanation generation next |
| Explanation route | Newly reviewed | Hardened in this continuation pass | `functions/api/questions/explain-rag.ts` now uses `gateway.callStructured`, validates `ExplanationSchema`, rejects stale fallback cache entries, and returns explicit unavailable errors on gateway/no-context failure; `functions/api/questions/explain-rag.test.ts` covers success and fail-closed behavior | Consumer unavailable-state handling and a persisted canonical explanation contract remain unproven | Inspect UI consumer handling and continue direct AI call census |
| Structured condition cards | Newly reviewed | Hardened in this continuation pass | `functions/api/conditions/[identifier]/structured.ts` now uses `gateway.callStructured` with `StructuredConditionSchema`; `functions/api/conditions/[identifier]/structured.test.ts` proves gateway success and trusted stored-field fallback | Other visualizer/vision AI utilities still need direct-call review | Continue direct-AI-call census and keep active condition-page regressions |
| Library answer endpoint | Newly reviewed | Hardened in this continuation pass | `functions/api/library/answer.ts` now uses `gateway.callText` for answer generation, secure endpoint logging, and the shared `getEmbedding` helper for retrieval; `functions/api/library/answer.test.ts` proves gateway success, searched-reference fallback, and no route-level direct provider fetch | Live UI behavior is not browser-smoked | Keep route regression and review live smoke when credentials exist |
| Admin condition enrichment | Newly reviewed | Hardened in this continuation pass | `functions/api/admin/enrich-condition.ts` now uses `gateway.callStructured` with `EnrichmentResponseSchema`; `functions/api/admin/enrich-condition.test.ts` proves only requested enrichable fields are written and gateway failures do not update content | Admin enrichment still relies on AI-generated clinical content and should stay admin-only with review/provenance gates | Keep regression coverage and continue the direct-AI-call census |
| Library/RAG embeddings | Newly reviewed | Partially hardened in this continuation pass | `functions/api/library/answer.ts`, `functions/api/library/semantic-search.ts`, `lib/services/ragContextService.ts`, `lib/services/search/hybridSearch.ts`, and `lib/search.ts` now call the shared `lib/gemini.ts#getEmbedding` helper instead of duplicating raw `embedContent` fetches | The helper still performs the provider call directly; question-embedding backfill and benchmarks remain separate direct embedding callers | Add first-class gateway embedding telemetry later if needed; keep helper boundary tests |
| Generated-question preview/staging contract | Newly reviewed | Partially consolidated in this continuation pass | `functions/api/_shared/generated-question-preview.ts` now centralizes preview-only metadata, stem/explanation normalization, staging persistence, and `staged_for_review` attribution; primary and RAG generation routes use it; `functions/api/_shared/generated-question-preview.test.ts`, `functions/api/questions/generate.test.ts`, and `functions/api/questions/generate-rag.test.ts` cover the shared contract | Enhanced generation and admin-only deep preview still have distinct storage/preview policies; full prompt/schema consolidation remains open | Continue toward a canonical generated-question schema/prompt adapter without changing serving gates |
| Enhanced-generation staging provenance | Newly reviewed | Hardened in this continuation pass | `functions/api/questions/generate-enhanced.ts` now fails closed when the staging/provenance write fails before live promotion; `functions/api/questions/generate-enhanced.test.ts` covers the no-live-write path | Enhanced generation still uses its own CoVe/live-promotion contract until the canonical generated-question schema exists | Keep enhanced route tests green and fold the route into the canonical schema adapter later |
| Deep-context question generation | Newly reviewed | Clarified in this continuation pass | `functions/api/questions/generate-deep.ts` remains admin-only preview, but now labels output `persistence: "admin_preview_only"` with `adminPreviewOnly: true`; `functions/api/questions/generate-deep.test.ts` proves the old `not_staged` label is absent | The route still uses text JSON parsing and should not be learner-facing until it has the canonical generated-question schema | Keep hidden/admin-only; move through canonical schema adapter before any learner exposure |
| Pre-generated submission | Improved | Approved-only mirror path exists | `functions/api/questions/attempt.ts`, `functions/api/questions/record.ts`, `functions/api/_shared/canonical-question-mirror.ts` | Historical identity remains split without schema source columns | Keep compatibility, plan migration/backfill |
| Study modes | Improved | `core_adaptive` and `system_drill` are launch candidates; many modes hidden/deferred | `STUDY_MODES_FUNCTIONALITY_AUDIT.md`, `lib/modes/modeReadiness.ts`, `config/lazyComponents.tsx` | Browser-level route/CTA smoke still missing | Add route smoke when runtime credentials are available; keep deferred modes hidden |
| Study-plan flow | Improved | Launch/completion and accepted-plan preservation are stronger | `DATA_PIPELINE_AND_SCHEDULING_AUDIT.md`, `functions/api/_shared/studyPlanService.ts`, `hooks/useStudyPlanLaunch.ts` | Full V2 contract still split across study-path/study-plan/daily-plan | Continue contract consolidation without breaking compatibility |
| Study-plan route scope | Newly reviewed | Hardened in this continuation pass | `functions/api/_shared/studyPlanService.ts` now emits `mode=system` for single-system plan tasks; `functions/api/_shared/studyPlanService.test.ts` covers stale adaptive route normalization | Multi-system tasks still launch as adaptive; condition-targeted and review task contracts remain split until V2 consolidation | Keep launch-route tests and continue StudyPlanTask V2 unification |
| FSRS/review scheduling | Improved | `drillReviewService` is canonical writer for active paths | `lib/services/drillReviewService.ts`, `functions/api/drills/submit-review.ts` | Legacy `/api/srs/*` shells and schema types remain | Keep shells until browser/runtime compatibility and migration cleanup are proven |
| Dashboard truth | Improved | Review coverage now requires plan identity matches | `components/dashboard/adaptive/engine`, `DATA_PIPELINE_AND_SCHEDULING_AUDIT.md` | Dashboard remains only as true as upstream plan/review identity | Keep adding plan/task identity tests |
| Medical database | Partial | Schema/content exists but provenance and canonical identity remain weak | `MEDICAL_DATABASE_FINAL_REPORT.md`, `prisma/schema.prisma` | Clinical completeness/provenance not launch-grade | Add seed/provenance gates separately from pipeline hardening |
| AI generation | Improved | Primary generation fails closed, enhanced CoVe-passed rows can be approved, RAG is preview/staging | `AI_LEARNING_ENGINE_FINAL_REPORT.md`, `functions/api/questions/generate*.ts` | No single canonical generated-question adapter across all generation paths | Continue adapter consolidation; keep batch/refill conservative |
| Deprecated code | Improved | Legacy dashboard and old palette removed in worktree; compatibility SRS remains | `DEPRECATED_CODE_AND_CONFLICTS_AUDIT.md`, `git status` | Dirty worktree includes many deletions; import census must stay clean | Do not remove more compatibility code until usage is proven |
| Deployment/runtime | Partial | Local validation strong | `FINAL_PRODUCTION_READINESS_REPORT.md` | No live Cloudflare/Clerk/Postgres smoke, scheduler ownership unresolved | Document smoke gate and avoid production actions |

## Fresh Reality Check Notes

- No `AGENTS.md` exists; project-specific constraints are in `CLAUDE.md` and skills.
- Production API code remains under `functions/api`; local `routes/` must not be treated as production truth.
- Worktree is intentionally dirty from prior large cleanup/hardening work. Push finalization restored the earlier staged deletion of `prisma/migrations/20260426000000_osce_factorization/migration.sql`, so historical migration files are preserved.
- Current continuation implementation should avoid migrations, destructive scripts, production deploys, and broad rewrites.
- Fresh targeted verification added for `functions/api/questions/context.ts`, `functions/api/questions/explain-rag.ts`, `functions/api/conditions/[identifier]/structured.ts`, `functions/api/library/answer.ts`, `functions/api/admin/enrich-condition.ts`, `functions/api/questions/generate-rag.ts`, primary/RAG generated-question preview staging, enhanced-generation staging provenance, study-plan launch route scope, and shared library/RAG embedding helper consumers; combined route/module checks, typecheck, lint, build, and `git diff --check` pass for the latest slice.
