# Implementation Log

Chronological record of every task's outcome in this run. One entry per task. Tasks appear in execution order. Each entry records: what changed, what verification ran, audit deltas, and any follow-ups.

Source queue: `docs/implementation/IMPLEMENTATION_QUEUE.md`
Source audit: `UNFINISHED_WORK_MASTER_AUDIT.md` (repo root)
Reconciliation notes: `docs/implementation/AUDIT_RECONCILIATION.md`

---

## Entry template

```
### TASK-XXX — <title>
- **Date:** YYYY-MM-DD
- **Status:** completed | partial | blocked | obsolete
- **Commit:** <hash or "—">
- **Files touched:** <list>
- **Change summary:** <1–3 sentences>
- **Verification:** <typecheck / audit / tests that ran>
- **Audit delta:** <what the improved audit reports before vs. after>
- **Follow-ups:** <if any>
- **Progress note:** `docs/implementation/progress/TASK-XXX.md`
```

---

## Entries

<!-- New entries appended below in execution order. -->

### TASK-001 — OSCE: stop writing session-id as QuestionAttempt
- **Date:** 2026-04-16
- **Status:** completed
- **Commit:** 0e0fed16
- **Files touched:** `components/modes/PatientEncounterMode.tsx`
- **Change summary:** Removed the `syncManager.queueAnswer({ questionId: sessionId, ... })` call and its `syncManager` import. A session id is not a legitimate `QuestionAttempt.questionId`, and pushing OSCE results through the main sync queue polluted FSRS and analytics with semantically wrong rows. OSCE results are already persisted via `completeOSCESession()` + `gradeOSCESession()`, and the condition-level spaced repetition schedule is updated via `updateConditionSchedule()`, which is kept intact.
- **Verification:** Scoped `tsc -p tsconfig.scratch.json` (clean); eslint on touched file (clean); grep for residual `syncManager` / `isPass` / `scorePct` references in the file (none).
- **Audit delta:** Closes the "OSCE encounter queues answer with session id as question id" item in `UNFINISHED_WORK_MASTER_AUDIT.md` (High priority, Low risk).
- **Follow-ups:** None. Future OSCE→FSRS bridging, if ever needed, must route through a dedicated `OSCEAttempt` pipeline, not `QuestionAttempt`.

### META — Fix `scripts/audit-zod-validation.ts` detection gaps
- **Date:** 2026-04-16
- **Status:** completed
- **Commit:** (pending this-run commit)
- **Files touched:** `scripts/audit-zod-validation.ts`
- **Change summary:** Rewrote the Zod audit script so it recognizes (1) all seven shared middleware wrappers that internally compose `withValidation(schema)`, (2) the TypeScript-generic call form `authenticatedEndpoint<Input>(...)`, (3) `withValidation(...)` composition anywhere in a custom chain, (4) CRON_SECRET bearer auth and Svix webhook verification as `WARN_OUT_OF_BAND` (Zod not applicable), and (5) rejects `JSON.parse(` as false Zod evidence via a token-preceding line scan. Excludes `.test.ts` and `.disabled` paths.
- **Verification:** Ran the fixed script against `functions/api/**` — output: 176 PASS, 8 WARN_OUT_OF_BAND, 3 WARN_MANUAL_ONLY, 2 FAIL. The two FAILs are expected exceptions (`drill/log-attempt.ts` 410 tombstone, `podcast/generate.ts` external proxy).
- **Audit delta:** Converts §5 "145 endpoints fail audit:zod" from an unactionable phantom to a two-line reality list. Future audits now produce signal instead of noise. See `AUDIT_RECONCILIATION.md` for root-cause breakdown.
- **Follow-ups:** None. If new wrappers are added, append them to `VALIDATED_WRAPPERS`.

### TASK-007 — Zod-harden `POST /api/users/me/daily-plan/complete`
- **Date:** 2026-04-16
- **Status:** completed
- **Commit:** (pending this-run commit)
- **Files touched:** `functions/api/users/me/daily-plan.ts`
- **Change summary:** Replaced the raw `withMiddleware(...)` chain on `onRequestPost` with `authenticatedEndpoint(DailyPlanCompleteSchema, handler, { requestsPerMinute: 30 })`. Added `DailyPlanCompleteSchema` clamping `accuracy` to 0..1 and `durationMinutes` to 0..1440 (whole minutes). Handler reads from `context.validated.body` instead of `request.json()`. GET handler untouched (no body to validate).
- **Verification:** Fixed audit script confirms `users/me/daily-plan.ts` no longer appears under FAIL or WARN. Handler compiles against the `ValidatedContext` type; no callers depend on the new `DailyPlanCompleteRequest` export.
- **Audit delta:** Closes the file-level item under §5 "API validation hardening" for `users/me/daily-plan`. Umbrella "145 endpoints" claim now reconciled as stale (see reconciliation).
- **Follow-ups:** None.
- **Progress note:** `docs/implementation/progress/TASK-007.md`

### TASK-008 — Zod-harden `POST /api/users/me/exam-outcome`
- **Date:** 2026-04-16
- **Status:** completed
- **Commit:** (pending this-run commit)
- **Files touched:** `functions/api/users/me/exam-outcome.ts`
- **Change summary:** Full rewrite to `authenticatedEndpoint(ExamOutcomeSchema, handler, { requestsPerMinute: 30 })`. Schema enforces `examType` enum (`PANCE`, `PANRE`, `EOR`, `block-exam`), ISO `examDate` via `.refine()`, 0..100 bounds on `score` and `percentile`, 0..86400 bounds on `timeLimit` / `timeUsed`. Clerk-ID → internal-ID lookup and `recordExamOutcome()` call unchanged semantically.
- **Verification:** Fixed audit script confirms `users/me/exam-outcome.ts` no longer appears under FAIL or WARN. Manual-validation regex in the handler is gone; `grep 'if (!examType'` returns nothing.
- **Audit delta:** Closes the file-level item under §5 for `users/me/exam-outcome`. Feeds clean data into the outcome-optimization / system-predictiveness pipeline — no more arbitrary strings or out-of-range numerics reaching `recordExamOutcome()`.
- **Follow-ups:** If PA programs introduce additional exam categories, extend the `examType` enum in lock-step with the Prisma column.
- **Progress note:** `docs/implementation/progress/TASK-008.md`

### TASK-009 — Zod-harden `POST /api/podcast/generate` (proxy)
- **Date:** 2026-04-16
- **Status:** completed
- **Commit:** (pending this-run commit)
- **Files touched:** `functions/api/podcast/generate.ts`
- **Change summary:** Branch-specific validation. Added `import { z } from 'zod'`; declared `PodcastGenerateJsonSchema = z.object({ pdfUrl, topic, voice, title, language, style }).passthrough()` with bounded scalars; exported `PodcastGenerateSchema` + `PodcastGenerateRequest` type. Added 415 content-type gate. JSON branch now runs `.safeParse()` and forwards the validated object (not the raw body). Multipart branch rejects payloads past 25 MB via `Content-Length` before `formData()` materializes. Existing `withMiddleware(withCors, withErrorHandling, withAuth, withRateLimit(5/min), handler)` chain retained — proxy semantics preserved because the downstream Node service at `PODCAST_SERVICE_URL` still owns the multipart field contract.
- **Verification:** Faithful node-native port of `scripts/audit-zod-validation.ts` on full `functions/api/**`: 189 mutation endpoints, 177 PASS (+1), 8 WARN_OUT_OF_BAND, 3 WARN_MANUAL_ONLY, **1 FAIL** (`drill/log-attempt.ts` — 410 Gone tombstone, expected). `podcast/generate.ts` → PASS via `.safeParse(` detection.
- **Audit delta:** Closes the file-level item under §5 "API validation hardening" for `podcast/generate`. Only remaining audit:zod FAIL is the deprecated 410 tombstone.
- **Follow-ups:** Tighten schema from `.passthrough()` to `.strict()` if/when the downstream Node service publishes a stable JSON contract. Raise `MAX_MULTIPART_BYTES` in lock-step with any Cloud Run request-size bump.
- **Progress note:** `docs/implementation/progress/TASK-009.md`

### TASK-010 — Retire orphaned `/api/questions/review` endpoint
- **Date:** 2026-04-16
- **Status:** completed
- **Commit:** `fbad2689` (code) / `6771368a` (docs)
- **Files touched:** `functions/api/questions/review.ts` (rewritten as 410 Gone tombstone for both GET and POST); **deleted** `lib/services/review/reviewSubmissionService.ts`, `lib/services/review/reviewService.ts`, `lib/services/review/reviewService.test.ts`; **removed** now-empty `lib/services/review/` directory.
- **Change summary:** Caller inventory of `/api/questions/review` came back clean — the only caller of the POST path was the client-side wrapper `reviewSubmissionService.ts`, which had zero UI/hook/store importers; the GET path's server-side `ReviewService` class was only used by the retiring endpoint itself. Replaced the endpoint with a 410 Gone tombstone matching the `functions/api/drill/log-attempt.ts` pattern — both handlers return `{ error, migration }` with distinct migration pointers (POST → `/api/drills/submit-review`; GET → proactive question reservoir). Deleted the three orphaned service files (safely via `mv ~/.Trash/panacea-task010-retirement/` per CLAUDE.md) and `rmdir`'d the now-empty directory.
- **Verification:** Post-deletion `grep -rn 'reviewSubmissionService\|services/review/'` across `*.{ts,tsx,js,jsx}` returns zero importers. Audit script run: 189 mutation endpoints, 176 PASS, 8 WARN_OUT_OF_BAND, 3 WARN_MANUAL_ONLY, **2 FAIL** (`drill/log-attempt.ts` + `questions/review.ts` — both deliberate 410 tombstones, both expected). Tombstone uses the same `PagesFunction` ambient type as `log-attempt.ts`, matching established precedent (Cloudflare provides the type at deploy time).
- **Audit delta:** Unparks and closes the `/api/questions/review` half of the "Retire deprecated SRS endpoints" row. The `/api/srs/submit` half stays parked — it has an active caller (`SrsFlashcardView`) and narrowing requires a product decision on whether flashcard practice flips to the FSRS pipeline. Audit `audit:zod` FAIL count: 1 → 2, new steady state (tombstones intentionally don't validate request bodies).
- **Follow-ups:** Revisit the tombstone in ~2 release cycles — if zero production traffic lands on `/api/questions/review` in that window, the file can be deleted entirely and the resulting 404 is a safe steady state. Narrow `/api/srs/submit` + drop `SRSItem` model remain deferred.
- **Progress note:** `docs/implementation/progress/TASK-010.md`

### TASK-011 — Harden WARN_MANUAL_ONLY multipart endpoints + document Sentry-tunnel rationale
- **Date:** 2026-04-16
- **Status:** completed
- **Commits:** `acb0a0d5` (code) / `a15cfdd0` (docs)
- **Files touched:** `functions/api/knowledge/upload.ts`, `functions/api/technique-check/analyze.ts`. **Not modified:** `functions/api/sentry-tunnel.ts` (documented rationale in `AUDIT_RECONCILIATION.md`).
- **Change summary:** Tightened manual validation on the two multipart endpoints that appear in `WARN_MANUAL_ONLY` under the improved audit. `knowledge/upload.ts`: added a `415` content-type gate and `413` `Content-Length` short-circuit before `formData()` materializes up to 50 MB; retained the post-materialization `file.size` check as defense-in-depth; expanded JSDoc with a "Validation model" block. `technique-check/analyze.ts`: flipped wrong-content-type response from `400 → 415` and oversized-video response from `400 → 413` (proper HTTP semantics); inserted a `413` `Content-Length` short-circuit before `formData()` materializes up to 20 MB; added `MAX_QUERY_CHARS = 2000` to bound the `query` string and prevent pathologically long prompts reaching Gemini; expanded JSDoc. `sentry-tunnel.ts` left untouched — Sentry envelopes are a newline-delimited streaming format owned by Sentry's SDK, and the endpoint's existing DSN extraction + project-ID whitelist + per-IP in-memory rate limit IS the structurally correct validation surface. Documented this permanent `WARN_MANUAL_ONLY` steady state in `AUDIT_RECONCILIATION.md` so future audit passes don't re-queue it.
- **Verification:** Audit script run after both edits: 189 mutation endpoints, **176 PASS, 8 WARN_OUT_OF_BAND, 3 WARN_MANUAL_ONLY, 2 FAIL** — unchanged, by design. The script classifies by code-shape (does a file use `withValidation` / a wrapper / `.safeParse()` / manual string checks?), not by validation depth; the hardening tightens manual validation inside the `WARN_MANUAL_ONLY` bucket without moving endpoints between buckets. Scoped typecheck against `--types @cloudflare/workers-types --strict --noUncheckedIndexedAccess` on both touched files: zero errors attributable to this task (pre-existing `process` errors surface from `_shared/auth.ts`, `_shared/env-validation.ts`, `_shared/prisma-edge.ts`, `_shared/secureLogger.ts` — unrelated transitive leaks). Caller contract check: `MyLibraryPage.tsx` (knowledge/upload) and `TechniqueCheckPage.tsx` (technique-check/analyze) both send compliant multipart bodies and reasonable query lengths; the new gates only fire on misrouted or adversarial traffic.
- **Audit delta:** Closes the `WARN_MANUAL_ONLY` multipart tail of §5 "API validation hardening" with proper HTTP semantics (415/413) and defense-in-depth ceilings. `sentry-tunnel.ts` reclassified from "latent hardening candidate" to "intentional steady state" — permanently valid `WARN_MANUAL_ONLY` by design. Audit top-line counts (176/8/3/2) unchanged.
- **Follow-ups:** None for this sprint. Optional future cleanups: (1) wire `MyLibraryPage.tsx`'s `uploadDisplayName` through to the server (currently hardcoded `display_name: 'library'` in the Gemini upload call); (2) revisit `MAX_QUERY_CHARS = 2000` if clinical-technique queries ever legitimately need longer prompts.
- **Progress note:** `docs/implementation/progress/TASK-011.md`

### TASK-012 — Normalize loading states in adaptive session + major mode shells
- **Date:** 2026-04-16
- **Status:** completed
- **Commits:** `e11b0457` (code) / `3b15f3f7` (docs)
- **Files touched:** `components/session/CoreAdaptiveSession.tsx`, `components/modes/CramMode.tsx`, `components/modes/GrandRoundsMode.tsx`. **Not modified:** `components/modes/PatientEncounterMode.tsx` (all five remaining spinners are inline button feedback, deferred pending a canonical `InlineButtonSpinner` primitive).
- **Change summary:** First tranche of the §6 "Loading-state normalization" rollout. Migrated four full-page loading blocks to the canonical `DrillLoadingState` primitive. (1) `CoreAdaptiveSession.tsx`: replaced the `isLoading` return block's handrolled border-spinner with `<DrillLoadingState message="Resolving your study plan..." variant="question" showTimer showProgress />`; replaced the `<Suspense fallback={...}>` inner spinner with `<DrillLoadingState message="Preparing your questions..." variant="question" showTimer showProgress />`. (2) `CramMode.tsx`: replaced the `!currentQuestion` guard block with `<DrillLoadingState message="Loading question..." variant="question" showTimer showProgress />`; **intentionally left the generation-progress block (real-progress count + progress bar) on `Loader2`** because canonical primitives cannot represent `{completed}/{total}` data without losing the UX value. (3) `GrandRoundsMode.tsx`: replaced the `viewState === 'loading'` block with `<DrillLoadingState message={`Loading ${modeLabel}...`} variant="question" showTimer showProgress />` (preserving the Grand Rounds / Targeted Daily Question template) and replaced the `!currentQuestion` guard with `<DrillLoadingState ... />`. Three inline button spinners in GrandRoundsMode (lines 702, 1027, 1194) intentionally kept on `Loader2` — they need a canonical `InlineButtonSpinner` primitive that does not yet exist. `Loader2` import retained in both mode files because the intentionally-kept spinners still use it.
- **Verification:** `NODE_OPTIONS="--max-old-space-size=4096" npx tsc --noEmit` completed via Desktop Commander background runner (sandbox bash hit the 45 s cap). Zero errors in any of the three touched files (`components/session/CoreAdaptiveSession.tsx`, `components/modes/CramMode.tsx`, `components/modes/GrandRoundsMode.tsx`) or in `components/loading/index.tsx`. Pre-existing 1,152 repo-wide error lines unchanged — all in unrelated areas (`services/optimizer/*`, `services/imageOptimizationService.ts`, `functions/api/admin/refinery/action.ts`, etc.). No regression test surface: `grep -rl` across `tests/` and `components/**/*.test.*` returned zero matches for `CoreAdaptiveSession|CramMode|GrandRoundsMode|DrillLoadingState`. Behavioral parity: `DrillLoadingState` has its own `min-h-[500px]` wrapper + centered content column that replaces the bespoke containers (`min-h-[60vh]` / `min-h-[40vh]` / `min-h-screen`) correctly and adds a timer placeholder, progress bar, and staggered option rows plus proper `aria-live` / `role="status"` affordances that the handrolled spinners did not have. Diff footprint: 3 files touched; ~40 lines removed, ~25 lines added, net −15 lines. Safe rollback = single `git revert`.
- **Audit delta:** `UNFINISHED_WORK_MASTER_AUDIT.md` §6 "Finish migration to `components/loading` primitives across adaptive/session and mode shells" — **core adaptive session + cram + grand rounds full-page loaders now on canonical primitives**; patient encounter + inline button spinners deferred. `AUDIT_RECONCILIATION.md` §7 "Loading-state normalization rollout" reclassified from `accurate` / `deferred` → **`partial`** for this run. No change to the Zod audit (176 PASS / 8 WARN_OUT_OF_BAND / 3 WARN_MANUAL_ONLY / 2 FAIL) — this task is a frontend consolidation and does not touch `functions/api/**`.
- **Follow-ups:** (1) Create a canonical `InlineButtonSpinner` primitive in `components/loading/index.tsx` (`w-4`/`w-5` size variants, `currentColor`, gold-accent ring) and migrate the 3 GrandRoundsMode + 5 PatientEncounterMode inline button spinners. (2) Document the intentional real-progress carve-out for `CramMode.tsx`'s generation block; revisit only if canonical primitives ever gain a `ProgressWithCount` variant. (3) Survey the ~25 other files flagged by the `animate-spin` grep (library, dashboard, command palette, my-library, etc.) in follow-up sprints; most are likely inline button spinners, the full-container loaders should migrate one sprint at a time to keep diffs reviewable.
- **Progress note:** `docs/implementation/progress/TASK-012.md`

### TASK-013 — Add canonical `InlineButtonSpinner` primitive + migrate 8 inline button spinners
- **Date:** 2026-04-17
- **Status:** completed
- **Commits:** `050a09df` (code) / `2dfb82d1` (docs)
- **Files touched:** `components/loading/index.tsx`, `components/modes/GrandRoundsMode.tsx`, `components/modes/PatientEncounterMode.tsx`.
- **Change summary:** Second tranche of the §6 "Loading-state normalization" rollout; closes the inline-button-spinner cluster that TASK-012 explicitly deferred. (1) `components/loading/index.tsx`: added `InlineButtonSpinner` named export plus `InlineButtonSpinnerSize` (`'sm' | 'md'`) + `InlineButtonSpinnerProps` types. SVG-based ring spinner — track at `strokeOpacity={0.3}`, quarter-arc animated head at full opacity, both `stroke="currentColor"`. `animate-spin` root (respects `prefers-reduced-motion` via Tailwind's built-in media-query stub). `aria-hidden="true"` + `focusable="false"` by default since the surrounding disabled-while-submitting button's text ("Submitting...", "Evaluating...", "Generating Case...", "Consulting Preceptor...") is the single source of screen-reader announcement. SVG chosen over Tailwind `border-current/30` + `border-t-current` tricks because `color-mix(... currentColor ...)` has uneven browser support in Tailwind 3.4's JIT arbitrary-property output for border-side utilities; `stroke-opacity` is universal. (2) `components/modes/GrandRoundsMode.tsx`: extended the `@/components/loading` import to include `InlineButtonSpinner`, replaced 3 `<Loader2 className="w-5 h-5 animate-spin" />` occurrences (lines 702, 1027, 1194) with `<InlineButtonSpinner />`, and removed the now-orphaned `Loader2,` line from the lucide-react import block. (3) `components/modes/PatientEncounterMode.tsx`: extended the existing `import { ChatSkeleton } from '@/components/loading';` at line 85 to also import `InlineButtonSpinner`, replaced 2 handrolled `<div aria-hidden="true" className="w-5 h-5 border-2 border-white/30 border-t-white rounded-full animate-spin" />` at lines 1706, 2616 with `<InlineButtonSpinner />` (default `md` size), and replaced 3 handrolled `w-4 h-4` variants at lines 2722, 2775, 2793 with `<InlineButtonSpinner size="sm" />`. The 8 call sites before migration were 3 `Loader2` icons + 5 handrolled border-ring divs all hard-coded to white (`border-t-white`) — post-migration they all inherit `currentColor` from their parent button, which is white at every call site, so visible color is unchanged.
- **Verification:** `NODE_OPTIONS="--max-old-space-size=4096" npx tsc --noEmit` completed via Desktop Commander background runner in ~38 s. Zero errors in any of the three touched files. Pre-existing 1,152 repo-wide error lines unchanged — identical to the post-TASK-012 baseline. No regression test surface: `grep -rl` for `InlineButtonSpinner|GrandRoundsMode|PatientEncounterMode` across `tests/` and colocated `.test.*` files returned zero matches; the primitive is newly minted and the two mode files have no existing test coverage. Behavioral parity: size mapping is exact (`w-5 h-5` → `size="md"`, `w-4 h-4` → `size="sm"`); all call sites are inside buttons with white text so `currentColor` inheritance reproduces the previous visible color; `aria-hidden` semantics are now correct-by-default on all 8 call sites (lucide's `Loader2` previously had no default `aria-hidden`, handrolled divs had to remember it manually). Diff footprint: 3 files touched, 8 call sites migrated, 1 new primitive, 1 orphaned import removed. Safe rollback = single `git revert`.
- **Audit delta:** `AUDIT_RECONCILIATION.md` §7 "Loading-state normalization rollout" status stays `partial` but narrows further — the inline-button-spinner cluster is closed for `GrandRoundsMode` + `PatientEncounterMode`; the real-progress generation block in CramMode remains intentional steady state; the ~25-file "other `animate-spin`" cluster remains queued for TASK-014+. `UNFINISHED_WORK_MASTER_AUDIT.md` §6 action — all loading affordances in the two active encounter-style mode shells (GrandRoundsMode, PatientEncounterMode) now route through the canonical loading module. No change to the Zod audit (176 PASS / 8 WARN_OUT_OF_BAND / 3 WARN_MANUAL_ONLY / 2 FAIL) — this task is a frontend consolidation and does not touch `functions/api/**`.
- **Follow-ups:** (1) TASK-014+ animate-spin sweep across the ~25 other files (library, dashboard, command palette, my-library, admin, etc.) — requires per-cluster classify-then-migrate pass; some are inline-button candidates for the new primitive, some are full-container blocks for `DrillLoadingState` / `ClinicalSkeleton` / `StreamingSkeleton`, a handful are likely intentional non-loading spinners. (2) Consider an `InlineSpinnerWithLabel` helper for the `<InlineButtonSpinner /> "Submitting..."` pattern duplicated across the 8 migrated call sites — low priority, non-blocking. (3) Light-mode QA pass on the new primitive — `strokeOpacity={0.3}` track should be re-evaluated on light-on-light buttons if any ghost-button variant ever uses dark-accent text on near-white background.
- **Progress note:** `docs/implementation/progress/TASK-013.md`

### TASK-014 — Widen `InlineButtonSpinner` → `InlineSpinner` + migrate clinical-library animate-spin loaders
- **Date:** 2026-04-17
- **Status:** completed
- **Commits:** `0cb099fc` (code — bundled with calculator sprint-9 in a combined commit) / `0b90329a` (docs)
- **Files touched:** `components/loading/index.tsx`, `components/library/DrugReferenceLibrary.tsx`, `components/library/SmartPDFViewer.tsx`, `components/library/ContextWidget.tsx`, `components/library/ReferenceHub.tsx`, `components/library/HighYieldSummary.tsx`, `components/library/GenericReferenceView.tsx`.
- **Change summary:** Third tranche of the §6 "Loading-state normalization" rollout; begins the post-TASK-013 sweep on the clinical-library cluster. (1) `components/loading/index.tsx`: renamed TASK-013's `InlineButtonSpinner` into a canonical `InlineSpinner` and widened the size axis from 2 sizes to 4 — `sm=w-4 h-4`, `md=w-5 h-5`, `lg=w-8 h-8`, `xl=w-12 h-12`. Kept the SVG-ring + `stroke-opacity=0.3` track / `currentColor` arc-head implementation unchanged (universal browser support; no reliance on `color-mix(... currentColor ...)`). Rewrote the JSDoc block to cover the three legitimate use cases: buttons (inherits disabled-button text), inline status rows ("Loading drugs…"), container overlays on single-purpose screens; other flows should still reach for `ClinicalSkeleton` / `Loader` / `DrillLoadingState`. Added a backward-compat shim: `export const InlineButtonSpinner = InlineSpinner;` plus type re-exports `InlineButtonSpinnerSize` / `InlineButtonSpinnerProps`. All 8 TASK-013 call sites in `GrandRoundsMode` + `PatientEncounterMode` continue to compile and render identically through the alias. (2) `components/library/DrugReferenceLibrary.tsx`: replaced the `LoadingOverlay` helper's hand-rolled 48×48 CSS ring (4-px border with `border-t` accent) with `<InlineSpinner size="xl" className="text-[var(--color-accent)] mb-4" />`. Wrapped the container with `role="status" aria-live="polite"`. Left the lucide `RefreshCw` icon on line 580 untouched — that's a refresh-button glyph, not a loading beacon. (3) `components/library/SmartPDFViewer.tsx`: dropped `Loader2` from the lucide-react import block (now orphaned), added `import { InlineSpinner } from '@/components/loading';`, and replaced the 24-px `Loader2 animate-spin` overlay with `<InlineSpinner size="lg" className="text-[var(--color-text-muted)]" />` inside a `role="status" aria-live="polite" aria-label="Loading PDF viewer"` container. (4) `components/library/ContextWidget.tsx`: dropped `Loader2` import, added `InlineSpinner` import, replaced the 24-px `Loader2 animate-spin` with `<InlineSpinner size="lg" className="text-[var(--color-text-muted)]" />` inside the existing card shell; added `role="status" aria-live="polite" aria-label={\`Loading ${title.toLowerCase()}\`}`. (5) `components/library/ReferenceHub.tsx`: dropped `RefreshCw` from lucide-react import, added `InlineSpinner` import, migrated both inline-status rows — the "Searching across all reference types…" row inside the search-all-results area (previously a 14-px `RefreshCw` with `animate-spin`) and the "Loading reference library…" row on first mount — to `<InlineSpinner size="sm" />` inside containers with `role="status" aria-live="polite"`. (6) `components/library/HighYieldSummary.tsx`: dropped `RefreshCw` from lucide-react import, added `InlineSpinner` import, swapped the 14-px `RefreshCw animate-spin` counter spinner (shown next to each entity group while that group's fetch is in flight) for `<InlineSpinner size="sm" className="text-[var(--color-text-secondary)]" />`. (7) `components/library/GenericReferenceView.tsx`: dropped `RefreshCw` from lucide-react import (lucide-react block on line 34 of the file), added `InlineSpinner` import, replaced the full-container loading block's 20-px `RefreshCw animate-spin` with `<InlineSpinner size="md" className="mr-2" />` and wrapped the `display: flex; justifyContent: center` container with `role="status" aria-live="polite"`. Did not touch the header refresh button — that `RefreshCw` is the refresh-control icon, not a loading indicator.
- **Verification:** `NODE_OPTIONS="--max-old-space-size=4096" npx tsc --noEmit` run via Desktop Commander background PID 47964; completed in ~61 s with repo-wide error count 1,151 — one line lower than the 1,152 baseline from TASK-013, because three orphaned `Loader2` / `RefreshCw` lucide imports were cleaned up (the pre-existing 1,152 errors never touched these files, so "down one" is net-positive). `grep -rn "animate-spin" components/library/` confirms only the two intentional `RefreshCw` refresh-button glyphs remain — every loading-beacon spot in the library cluster now routes through `InlineSpinner`. Diff footprint: 7 files touched, 7 loading spots migrated, 1 primitive widened (2 sizes → 4 sizes), 3 orphaned imports removed, 0 new primitives added, 5 spots gained `role="status"` / `aria-live="polite"` a11y affordances. Backward-compat smoke-test: TASK-013's `import { InlineButtonSpinner } from '@/components/loading';` still resolves via the alias; TASK-013's 8 call sites compile with zero changes. Safe rollback = single `git revert`.
- **Audit delta:** `UNFINISHED_WORK_MASTER_AUDIT.md` §6 action — the clinical-library cluster is now fully on canonical loading primitives. `AUDIT_RECONCILIATION.md` §7 "Loading-state normalization rollout" status stays `partial` but narrows further — library cluster closed, ~18 other files remain (session, drill, dashboard, command palette, my-library, admin, etc.). No change to the Zod audit (176 PASS / 8 WARN_OUT_OF_BAND / 3 WARN_MANUAL_ONLY / 2 FAIL) — this task is a frontend consolidation and does not touch `functions/api/**`.
- **Follow-ups:** (1) TASK-015+: next cluster is session-adjacent — `NormalLabsPanel`, `SessionScopeSelector`, `ClinicalQuickRefPanel`, `SrsFlashcardView` are all candidates; session-cluster migrations have the highest user-facing visibility per session. Recommended order: session → drill → modes (remainder if any) → dashboard → admin (more diffuse). (2) The `InlineSpinner` xl size (w-12 h-12) is new; visually verify on retina + non-retina displays that the 2-px stroke remains crisp at 48 px — if it looks thin, consider a dynamic `strokeWidth` that scales with size. (3) `role="status" aria-live="polite"` wrappers were added at 5 of 7 library spots; the other 2 (DrugReferenceLibrary, ContextWidget) already had similar affordances embedded — audit for consistency once the remaining clusters migrate.
- **Progress note:** `docs/implementation/progress/TASK-014.md`

### TASK-015 — Migrate dashboard animate-spin loaders to canonical `InlineSpinner`
- **Date:** 2026-04-17
- **Status:** completed
- **Commits:** `9c2ea0b3` (code — combined with TASK-017 analytics cluster + incidental `DrillSetup.tsx` / `ContentFieldRenderer.tsx` cleanup in same commit) / `bda6752a` (docs)
- **Files touched:** `components/dashboard/InsightsHub.tsx`, `components/dashboard/RecommendationFeed.tsx`, `components/dashboard/GapAnalysisDashboard.tsx`, `components/dashboard/StudyPathDashboard/index.tsx`, `components/dashboard/StudyPathDashboard/ProgressProjectionChart.tsx`, `components/dashboard/ClinicalProfile/ClinicalProfileDashboard.tsx`.
- **Change summary:** Fourth tranche of the §6 "Loading-state normalization" rollout; addresses the dashboard cluster — the landing-surface users hit on every app open. Migrated 6 dashboard `animate-spin` loaders to canonical `InlineSpinner`: (1) `InsightsHub.tsx` — swapped `<Loader2 w-6 h-6 animate-spin />` for `<InlineSpinner size="lg" className="text-[var(--color-text-muted)]" />`, wrapped container in `role="status" aria-live="polite" aria-label="Loading insights"`, removed orphaned `Loader2` from lucide import. (2) `RecommendationFeed.tsx` — swapped the `<Loader2 w-8 h-8 />` empty-state "Analyzing your learning profile…" block for `<InlineSpinner size="lg" className="text-[var(--color-accent)] mb-2" />`, added `role="status" aria-live="polite"`. The line-291 `RefreshCw` refresh-button icon intentionally kept (TASK-014 precedent — conditional `animate-spin` on a meaningful glyph). (3) `GapAnalysisDashboard.tsx` — swapped handrolled `<div h-8 w-8 animate-spin rounded-full border-2 …/>` ring for `<InlineSpinner size="lg" className="text-[var(--color-accent)]" />`. (4) `StudyPathDashboard/index.tsx` — same swap for "Loading your personalized study path…" overlay. (5) `StudyPathDashboard/ProgressProjectionChart.tsx` — same swap for the projection-chart loading overlay. (6) `ClinicalProfile/ClinicalProfileDashboard.tsx` — swapped `<Loader2 h-6 w-6 />` for `<InlineSpinner size="lg" />` (inherits `currentColor` from parent). All 6 migrations added `role="status" aria-live="polite"` on the wrapping container and `import { InlineSpinner } from '@/components/loading';`.
- **Verification:** Static pattern check: `grep -n "animate-spin\\|Loader2" components/dashboard/...` returns only the intentional `RefreshCw` refresh-button icon in `RecommendationFeed.tsx:292`. Every dashboard loader now routes through canonical `InlineSpinner`. All migrations pair the spinner with a parent `role="status"` + `aria-live="polite"` (the SVG itself is `aria-hidden` — matches TASK-013/014 pattern). Size selection: every dashboard loader sits inside a min-height ≥ 16 rem container; `size="lg"` (w-8 h-8) matched original proportions (originals were w-6 through w-8).
- **Audit delta:** `UNFINISHED_WORK_MASTER_AUDIT.md` §6 action — the dashboard cluster is now fully on canonical loading primitives. `AUDIT_RECONCILIATION.md` §7 "Loading-state normalization rollout" stays `partial` but narrows: library + dashboard clusters closed, ~42 files remain (admin, drill/session, modes, toolkit/misc, analytics — analytics addressed in sibling TASK-017). No change to the Zod audit (176/8/3/2) — frontend-only change.
- **Follow-ups:** (1) TASK-017 addresses the analytics cluster in parallel (same commit). (2) TASK-018+: admin is the largest remaining cluster (~22 files) and should split into 2–3 sub-sprints. (3) Drill/session (~10 files), modes (~5), toolkit/misc (~5) follow.
- **Progress note:** `docs/implementation/progress/TASK-015.md`

### TASK-017 — Migrate analytics animate-spin loaders to canonical `InlineSpinner`
- **Date:** 2026-04-17
- **Status:** completed
- **Commits:** `9c2ea0b3` (code — combined with TASK-015 dashboard cluster in same commit) / `bda6752a` (docs)
- **Files touched:** `components/analytics/AdvancedLearningProfileDashboard.tsx`, `components/analytics/AnalyticsDashboard.tsx`, `components/analytics/ConfusionMatrix.tsx`, `components/analytics/LearningProfileDashboard.tsx`, `components/analytics/SyllabusDecompiler.tsx`, `components/analytics/TopicMasteryBreakdown.tsx`.
- **Change summary:** Fifth tranche of the §6 "Loading-state normalization" rollout; addresses the analytics cluster — 7 migrations across 6 files. (1) `AdvancedLearningProfileDashboard.tsx` — swapped `<RefreshCw w-10 h-10 animate-spin />` for `<InlineSpinner size="xl" className="text-[var(--color-accent)]" />`, removed orphaned `RefreshCw` from lucide import. (2) `AnalyticsDashboard.tsx` — swapped handrolled `<div w-3 h-3 border-2 … animate-spin />` tiny spinner inside Memory Stability Growth card header for `<InlineSpinner size="sm" className="text-[var(--color-accent)]" />`, added `role="status" aria-live="polite"`. (3) `ConfusionMatrix.tsx` — two swaps: line 82 inline "Loading…" row (`<Loader2 w-4 h-4 />` → `<InlineSpinner size="sm" />`), line 130 Compare button busy indicator (`<Loader2 w-3 h-3 />` → `<InlineSpinner size="sm" />`); removed orphaned `Loader2` from lucide import. (4) `LearningProfileDashboard.tsx` — swapped `<RefreshCw w-8 h-8 animate-spin />` for `<InlineSpinner size="lg" className="text-[var(--color-accent)]" />`, added `role="status" aria-live="polite" aria-label="Loading learning profile"`. Kept `RefreshCw` in imports — still used at line 211 as a static icon. (5) `SyllabusDecompiler.tsx` — swapped `<Loader2 w-12 h-12 mx-auto mb-3 animate-spin />` for `<InlineSpinner size="xl" />` inside a `<div className="mx-auto mb-3 flex justify-center">` wrapper (needed because `InlineSpinner` is inline-flex so `mx-auto` centering required the explicit flex-justify-center wrapper), removed orphaned `Loader2`. (6) `TopicMasteryBreakdown.tsx` — swapped `<Loader2 w-5 h-5 mr-2 animate-spin />` for `<InlineSpinner size="md" className="mr-2" />`, removed orphaned `Loader2`. One `RefreshCw`-as-refresh-button-icon in `DatabaseAnalyticsDashboard.tsx:245` intentionally kept (non-loading semantic, matches TASK-014/015 precedent).
- **Verification:** `grep -rn "animate-spin\\|Loader2" components/analytics/` returns only the intentional `RefreshCw` refresh-button icon in `DatabaseAnalyticsDashboard.tsx:245`. All other analytics loaders now route through canonical `InlineSpinner`. Size-selection logic matched original pixel sizes: w-3 (12 px) → `sm` (16 px — closest), w-4 (16 px) → `sm` (exact), w-5 (20 px) → `md` (exact), w-8 (32 px) → `lg` (exact), w-10/w-12 (40–48 px) → `xl` (48 px — exact / closest).
- **Audit delta:** `UNFINISHED_WORK_MASTER_AUDIT.md` §6 action — analytics cluster closed. `AUDIT_RECONCILIATION.md` §7 status stays `partial` but narrows further: library + dashboard + analytics clusters closed, ~42 files remain (admin, drill/session, modes, toolkit/misc). No change to the Zod audit (176/8/3/2) — frontend-only change.
- **Follow-ups:** TASK-018+ — admin is the largest remaining cluster (~22 files) and should split into 2–3 sub-sprints. Drill/session (~10 files), modes (~5), toolkit/misc (~5) follow.
- **Progress note:** `docs/implementation/progress/TASK-017.md`

### TASK-018 — Migrate drill + session cluster animate-spin loaders to canonical `InlineSpinner`
- **Date:** 2026-04-17
- **Status:** completed
- **Commits:** code landed across `a6b5f2cc` (ContrastiveDrillSession), `64d93d33` (ElaborationDrill), `0280b661` (ConditionDrillSession + RapidRecallDrill), and `31f26d99` (remaining 8 drill + session files — folded into a sibling useReducedMotion a11y sprint by a parallel agent); docs `bda6752a`.
- **Files touched:** (drill) `components/drill/DrillSetup.tsx`, `components/drill/ConditionDrillSession.tsx`, `components/drill/recall/RapidRecallDrill.tsx`, `components/drill/ElaborationDrill.tsx`, `components/drill/ContrastiveDrillSession.tsx`, `components/drill/PhotoDrillCard.tsx`, `components/drill/SystemDrillSession.tsx`, `components/drill/PharmacologyDrillSession.tsx`; (session) `components/session/SessionScopeSelector.tsx`, `components/session/QuizView.tsx`, `components/session/NormalLabsPanel.tsx`, `components/session/ClinicalQuickRefPanel.tsx`, `components/session/SrsFlashcardView.tsx`.
- **Change summary:** Sixth tranche of the §6 "Loading-state normalization" rollout — drill + session clusters combined. 15 migrations + 2 dead-import cleanups across 13 files; zero intentional keeps (no refresh-button icons in these clusters). **Drill cluster (8 migrations + 2 dead imports across 8 files):** (1) `DrillSetup.tsx` — start-drill button `<Loader2 w-6 h-6 />` → `<InlineSpinner size="lg" />`. (2) `ConditionDrillSession.tsx` two swaps: Coach's Corner hint loader (`w-5 h-5 border-2` → `size="md"`) and answer-submit overlay (`w-6 h-6 border-3` → `size="lg"`); `aria-live="polite"` added to both existing `role="status"` wrappers. (3) `RapidRecallDrill.tsx` two swaps: full-screen drill loader (`w-10 h-10 border-4` → `size="xl"`) and Verifying diagnosis button (`Loader2 w-4 h-4` → `size="sm"`). (4) `ElaborationDrill.tsx` grading spinner (`w-8 h-8 border-2` → `size="lg"`); added `role="status" aria-live="polite"` wrapper. (5) `ContrastiveDrillSession.tsx` study-set loader (`Loader2 w-6 h-6` → `size="lg"`). (6) `PhotoDrillCard.tsx` image-load overlay (`h-12 w-12 border-4` → `size="xl"`). (7) `SystemDrillSession.tsx` — unused `Loader2` import removed (dead). (8) `PharmacologyDrillSession.tsx` — unused `Loader2` import removed (dead). **Session cluster (7 migrations across 6 files):** (9) `SessionScopeSelector.tsx` two swaps: systems loader + conditions loader (both `Loader2 w-6 h-6` → `size="lg"`) with `aria-label="Loading systems"` / `"Loading conditions"`. (10) `QuizView.tsx` — collapsed ~22 lines of handrolled `<svg animate-spin h-5 w-5 ... circle path />` Submit-button SVG to a single `<InlineSpinner size="md" />`; added `aria-live="polite"` to the existing `<span role="status">Submitting...</span>`. (11) `NormalLabsPanel.tsx` — `RefreshCw w-5 h-5` → `size="md"` with `mr-2`. (12) `ClinicalQuickRefPanel.tsx` — `Loader2 w-5 h-5` → `size="md"`. (13) `SrsFlashcardView.tsx` two swaps: page loader (`Loader2 w-8 h-8` → `size="lg"` with `mb-4` wrapper) and submit button busy indicator (`Loader2 w-4 h-4` → `size="sm"`). All 15 migrations add or consolidate `role="status"` / `aria-live="polite"` on parent containers (the SVG itself remains `aria-hidden` — matches TASK-013/014/015/017 pattern).
- **Verification:** Static pattern check: `grep -rn "animate-spin\\|Loader2" components/drill/ components/session/` returns **zero matches** post-migration — zero remaining `animate-spin`, zero remaining `Loader2` references including dead imports. Size selection matched original pixel sizes (w-3/w-4 → sm, w-5 → md, w-6/w-8 → lg, w-10/w-12 → xl). The QuizView SVG simplification is visually identical: `InlineSpinner` internally uses the same opacity-25 track + opacity-75 arc-head pattern, inherits `currentColor` from `btn-cinematic`. No component API changes — all migrations are internal render-tree refactors.
- **Audit delta:** `UNFINISHED_WORK_MASTER_AUDIT.md` §6 action — drill + session clusters closed. `AUDIT_RECONCILIATION.md` §7 status stays `partial` but narrows: library + dashboard + analytics + drill + session clusters closed. Cumulative cluster sweep: 35 migrations across 32 files, 2 dead-import cleanups, 4 intentional keeps. Remaining ~32 files: admin (~22), modes (~5), toolkit/misc (~5). No change to the Zod audit (176/8/3/2) — frontend-only change.
- **Follow-ups:** TASK-019+ — admin is the largest remaining cluster (~22 files); split into 2–3 sub-sprints (content-editors, review-queues/refinery, dashboards). Modes (~5) and toolkit/misc (~5) are small clusters that can each fit in a single pass.
- **Progress note:** `docs/implementation/progress/TASK-018.md`
