# Implementation Queue

Derived from `UNFINISHED_WORK_MASTER_AUDIT.md` (2026-04-16) and cross-checked against current code on 2026-04-16.

## Source-of-truth reconciliation notes

Before queueing tasks, I re-validated the audit's specific claims against current code and re-ran (improved, middleware-aware) versions of the repo-native audit tools. Findings that change the queue:

- **Audit's named "still flagged" Zod endpoints are already validated**: `functions/api/exam/start.ts`, `functions/api/exam/complete.ts`, `functions/api/feedback/submit.ts`, `functions/api/srs/sync.ts`, `functions/api/drills/submit-review.ts` all use `authenticatedEndpoint(Schema, handler)`, which internally runs `withValidation(schema)`. The original `scripts/audit-zod-validation.ts` had multiple detection gaps: it did not recognize the seven shared middleware wrappers (`authenticatedEndpoint`, `adminEndpoint`, `adminAuthenticatedEndpoint`, `aiEndpoint`, `refineryEndpoint`, `cmsEndpoint`, `publicEndpoint`), did not handle the TypeScript-generic call form `authenticatedEndpoint<Input>(...)`, did not recognize CRON_SECRET / Svix webhook as legitimate out-of-band security, and matched `JSON.parse(` as evidence of Zod. After the script fix, **real FAIL count drops to 2**: `drill/log-attempt.ts` (a deprecated 410-Gone tombstone that never reads the body) and `podcast/generate.ts` (a proxy that forwards to an external Node service).
- **Real file-level gaps that were accurate**: `users/me/daily-plan.ts` POST and `users/me/exam-outcome.ts` POST genuinely lacked Zod. Addressed this run as TASK-007 and TASK-008.
- **Prisma disconnect audit** flagged 3 files, all of which are false positives: `_shared/env-validation.ts` (match is in a JSDoc example comment), `_shared/prisma-user-scope.ts` (factory — caller owns disconnect), `functions/api/health.ts` (DOES disconnect, via an indirect `disconnect = ...; await disconnect(prisma)` pattern my regex missed). **Prisma disconnect cleanup is effectively complete.** The audit's "18 fails" is stale.
- **OSCE bogus `queueAnswer({ questionId: sessionId })`** — confirmed present at `components/modes/PatientEncounterMode.tsx:1015–1024`. Valid quick-win.

## This-run queue (ordered)

All tasks are scoped to the "Just Do It" bucket from `CLAUDE.md`: no schema migrations, no new production deps, no auth-middleware changes, no deploys.

| ID | Title | Category | Priority | Risk | Size | Depends on | Audit ref |
|---|---|---|---|---|---|---|---|
| TASK-001 | Remove bogus OSCE `queueAnswer({ questionId: sessionId })` write | Frontend bug | High | Low | S | — | §5 "OSCE mode still has an incorrect answer-queue write"; §10 Quick win #1 |
| TASK-002 | (STALE) `admin/staging/*` Zod validation | API hardening | — | — | — | — | Superseded: all four files already validated via `adminEndpoint`/`adminAuthenticatedEndpoint` wrapper (internally applies `withValidation(schema)`). Script detection gap. |
| TASK-003 | (STALE) `admin/content/*` + `admin/enrich-condition` Zod | API hardening | — | — | — | — | Superseded: validated via wrapper. Script detection gap. |
| TASK-004 | (STALE) `admin/media/approve`, `admin/refinery/action`, `admin/blueprint-coverage`, `admin/conditions/[id]/parent` Zod | API hardening | — | — | — | — | Superseded: validated via wrapper. Script detection gap. |
| TASK-005 | (PARTIAL → split) user-facing write Zod | API hardening | — | — | — | — | Split into TASK-007 (`daily-plan`) + TASK-008 (`exam-outcome`). `performance/record` was already wrapper-validated (generic-call form); `drill/log-attempt` is a 410 Gone tombstone that never reads body. |
| TASK-006 | (STALE) question-pipeline writes Zod | API hardening | — | — | — | — | Superseded: validated via wrapper. Script detection gap. |
| TASK-007 | Zod-harden `POST /api/users/me/daily-plan/complete` | API hardening | High | Low | S | — | §5 "API validation hardening"; §10 Quick win #2 |
| TASK-008 | Zod-harden `POST /api/users/me/exam-outcome` | API hardening | High | Low | S | — | §5 "API validation hardening"; §10 Quick win #2 |
| TASK-009 | Zod-harden `POST /api/podcast/generate` (proxy) | API hardening | Medium | Low | M | — | §5 "API validation hardening" — branch-specific validation: inline `.safeParse()` on JSON path (`.passthrough()` permissive schema), 415 content-type gate, 25 MB multipart size ceiling. External Node service retains authority on multipart field-level shape. |
| TASK-010 | Retire orphaned `/api/questions/review` endpoint (410 tombstone both methods) + delete 3 orphaned service files | Endpoint retirement / dead-code removal | Medium | Low | S | — | "Not doing now" row unparked: caller inventory was clean. `functions/api/srs/submit.ts` narrowing stays parked (active `SrsFlashcardView` caller). |
| TASK-011 | Harden WARN_MANUAL_ONLY multipart endpoints (`knowledge/upload`, `technique-check/analyze`) + document `sentry-tunnel` as intentional steady state | API hardening | Medium | Low | S | — | §5 "API validation hardening" WARN_MANUAL_ONLY tail. 415 content-type gates, 413 Content-Length short-circuits, bounded `query` string (technique-check). `sentry-tunnel` stays `WARN_MANUAL_ONLY` permanently — Sentry envelope format is structurally incompatible with Zod. |
| TASK-012 | Migrate session/mode full-page loaders to canonical `DrillLoadingState` (CoreAdaptiveSession, CramMode, GrandRoundsMode) | UI hygiene / design system consolidation | Medium | Low | S | — | §6 "Loading-state normalization rollout"; begins the rollout on the highest-traffic session/mode shells. Real-progress generation block in CramMode intentionally kept on `Loader2`; inline button spinners deferred pending future `InlineButtonSpinner` primitive. |
| TASK-013 | Add canonical `InlineButtonSpinner` primitive + migrate 8 inline button spinners (GrandRoundsMode ×3, PatientEncounterMode ×5) | UI hygiene / design system consolidation | Medium | Low | S | TASK-012 | §6 "Loading-state normalization rollout"; closes the inline-button-spinner cluster deferred by TASK-012. SVG-based, `currentColor`-inheriting, `sm`/`md` sizes. |
| TASK-014 | Widen `InlineButtonSpinner` → `InlineSpinner` (sm/md/lg/xl) + migrate clinical-library animate-spin loaders (7 spots across 6 files) | UI hygiene / design system consolidation | Medium | Low | S | TASK-013 | §6 "Loading-state normalization rollout"; begins the ~25-file post-TASK-013 sweep on the clinical-library cluster. Adds `lg`/`xl` sizes for full-container overlays and wires `role="status"` / `aria-live="polite"` on all 5 previously non-a11y spots. Backward-compat alias preserves TASK-013 call sites. |
| TASK-015 | Migrate dashboard-cluster animate-spin loaders to canonical `InlineSpinner` (6 spots across 6 files) | UI hygiene / design system consolidation | Medium | Low | S | TASK-014 | §6 "Loading-state normalization rollout"; fourth tranche. Dashboard is landing-surface traffic so it is prioritized first after library. Files: `InsightsHub`, `RecommendationFeed`, `GapAnalysisDashboard`, `StudyPathDashboard/index`, `StudyPathDashboard/ProgressProjectionChart`, `ClinicalProfile/ClinicalProfileDashboard`. One `RefreshCw` refresh-button icon kept. |
| TASK-017 | Migrate analytics-cluster animate-spin loaders to canonical `InlineSpinner` (7 spots across 6 files) | UI hygiene / design system consolidation | Medium | Low | S | TASK-015 | §6 "Loading-state normalization rollout"; fifth tranche. Files: `AdvancedLearningProfileDashboard` (xl), `AnalyticsDashboard` (sm), `ConfusionMatrix` ×2 (sm), `LearningProfileDashboard` (lg), `SyllabusDecompiler` (xl), `TopicMasteryBreakdown` (md). One `RefreshCw` refresh-button icon kept (`DatabaseAnalyticsDashboard`). |
| TASK-018 | Migrate drill + session cluster animate-spin loaders to canonical `InlineSpinner` (15 migrations + 2 dead-import cleanups across 13 files) | UI hygiene / design system consolidation | Medium | Low-Medium | M | TASK-017 | §6 "Loading-state normalization rollout"; sixth tranche. Drill files: `DrillSetup`, `ConditionDrillSession` ×2, `RapidRecallDrill` ×2, `ElaborationDrill`, `ContrastiveDrillSession`, `PhotoDrillCard`, `SystemDrillSession` (dead import), `PharmacologyDrillSession` (dead import). Session files: `SessionScopeSelector` ×2, `QuizView` (SVG handrolled simplification), `NormalLabsPanel`, `ClinicalQuickRefPanel`, `SrsFlashcardView` ×2. Zero intentional keeps (no refresh-button icons in these clusters). |

Evidence: after the script fix on 2026-04-16, TASK-002 through TASK-006 dissolved — the files are correctly validated via the shared middleware wrappers and were false positives of the original audit script. The only real file-level gaps were `users/me/daily-plan.ts` and `users/me/exam-outcome.ts` (now TASK-007 / TASK-008). See `docs/implementation/AUDIT_RECONCILIATION.md` for the per-claim reconciliation and the full audit-run deltas.

## Proposed execution order

1. TASK-001 — tiny, unblocks the "OSCE polluting sync/analytics" finding immediately. (done)
2. Fix `scripts/audit-zod-validation.ts` itself so follow-up audits do not keep generating false positives on wrapper-validated endpoints. (done)
3. TASK-007 + TASK-008 — user-facing mutation gaps surfaced by the fixed audit. (done)
4. TASK-009 — podcast/generate proxy gets branch-specific validation. (done; authorized past Ask First gate)
5. TASK-010 — `/api/questions/review` retirement. Caller inventory came back clean → both HTTP methods tombstoned as 410 Gone; 3 orphaned service files deleted. `/api/srs/submit` narrowing still parked (has active caller). (done)
6. TASK-011 — WARN_MANUAL_ONLY tail hardening. `knowledge/upload.ts` and `technique-check/analyze.ts` got 415 content-type gates, 413 `Content-Length` short-circuits, and proper HTTP semantics on oversized rejections. `technique-check/analyze.ts` also got a `MAX_QUERY_CHARS` bound. `sentry-tunnel.ts` documented as intentional `WARN_MANUAL_ONLY` steady state — Sentry envelope format is structurally incompatible with Zod, and its existing DSN + project-ID whitelist + IP rate-limit IS the right validation shape. (done)
7. TASK-012 — Loading-state normalization rollout, first tranche. Migrated the four highest-traffic full-page loading blocks (CoreAdaptiveSession `isLoading` + Suspense fallback, CramMode `!currentQuestion` guard, GrandRoundsMode `viewState === 'loading'` + `!currentQuestion` guard) to canonical `DrillLoadingState`. CramMode's real-progress generation block kept on `Loader2` (canonical primitives can't represent `{completed}/{total}` counts). Inline button spinners (3 in GrandRoundsMode, 5 in PatientEncounterMode) deferred pending a canonical `InlineButtonSpinner` primitive. (done)
8. TASK-013 — Loading-state normalization rollout, second tranche. Added canonical `InlineButtonSpinner` primitive to `components/loading/index.tsx` (SVG-based, `currentColor`-inheriting, `sm`/`md` sizes, `aria-hidden` by default). Migrated all 8 inline button spinners deferred by TASK-012: 3 in GrandRoundsMode (`Loader2`-based, import subsequently removed) and 5 in PatientEncounterMode (handrolled `border-t-white` divs). Typecheck: 0 new errors. (done)
9. TASK-014 — Loading-state normalization rollout, third tranche (clinical-library cluster). Widened TASK-013's `InlineButtonSpinner` to a 4-size canonical `InlineSpinner` (sm/md/lg/xl) and migrated 7 library-cluster animate-spin spots across 6 files (`DrugReferenceLibrary`, `SmartPDFViewer`, `ContextWidget`, `ReferenceHub` ×2, `HighYieldSummary`, `GenericReferenceView`). Two `RefreshCw`-as-refresh-button-icon instances in `DrugReferenceLibrary` (line 580) and `GenericReferenceView` (header refresh control) intentionally kept — non-loading semantic. Added `role="status"`/`aria-live="polite"` affordances to 5 spots that previously had none. Backward-compat alias on `InlineButtonSpinner` preserves all TASK-013 call sites. (done)
10. TASK-015 — Loading-state normalization rollout, fourth tranche (dashboard cluster). Migrated 6 dashboard animate-spin loaders across 6 files (`InsightsHub`, `RecommendationFeed`, `GapAnalysisDashboard`, `StudyPathDashboard/index`, `StudyPathDashboard/ProgressProjectionChart`, `ClinicalProfile/ClinicalProfileDashboard`). One `RefreshCw`-as-refresh-button-icon instance in `RecommendationFeed` (line 291) intentionally kept. All migrations pair with `role="status"` / `aria-live="polite"` on parents. (done)
11. TASK-017 — Loading-state normalization rollout, fifth tranche (analytics cluster). Migrated 7 analytics animate-spin loaders across 6 files (`AdvancedLearningProfileDashboard` xl, `AnalyticsDashboard` sm, `ConfusionMatrix` sm×2, `LearningProfileDashboard` lg, `SyllabusDecompiler` xl, `TopicMasteryBreakdown` md). One `RefreshCw`-as-refresh-button-icon instance in `DatabaseAnalyticsDashboard` (line 245) intentionally kept. (done)
12. TASK-018 — Loading-state normalization rollout, sixth tranche (drill + session clusters). Combined drill (8 migrations + 2 dead-import cleanups across 8 files) and session (7 migrations across 6 files) into one sprint; 15 total migrations + 2 dead imports across 13 files, zero intentional keeps. Includes QuizView's ~22-line handrolled SVG submit-button spinner collapsed to a single `<InlineSpinner size="md" />`. (done)
13. TASK-019+ — admin / modes / toolkit-misc clusters for remaining ~27 animate-spin files, library-enrichment `.disabled` endpoints, and architecture-level items (NotificationLog schema, runtime-owned push scheduler, Express-to-Edge retirement, study-groups decision). Admin is the largest remaining cluster (~22 files) — should split into 2–3 sub-sprints. Each still requires reading live callers before committing to a change shape.

## Not doing now — parked / deferred

| Item | Reason |
|---|---|
| NotificationLog schema + migration | Prisma migrate → Ask First per `CLAUDE.md`. Needs Aaron's approval. |
| Runtime-owned replacement for GitHub-cron push reminders | Requires `web-push` prod dep + architecture decision → Ask First. |
| Express-to-Edge retirement plan | Architecture change → Ask First; needs route-parity decision. |
| Study-groups/social build-or-freeze | Explicit product decision needed per audit §5. |
| Narrow `functions/api/srs/submit.ts` (drop `srsItemId` branch) + drop `SRSItem` model | Has active caller (`SrsFlashcardView`). Narrowing requires a product decision on whether flashcard practice flips to FSRS. `SRSItem` model drop is a Prisma migration → Ask First. (`functions/api/questions/review.ts` already retired this run — TASK-010.) |
| Library-enrichment admin endpoints re-enable | Two `.disabled` endpoints need data-source decision (files vs DB vs admin API). Ask First. |
| Dead-man-switch alerting | Deferred per audit §10; wait for automation ownership to settle. |
| Automated backup restore verification | Medium-risk ops lane; needs runbook before wiring into weekly maintenance. |
| Loading-state normalization rollout — remaining scope | Tranches done: TASK-012 (4 full-page loaders in CoreAdaptiveSession/CramMode/GrandRoundsMode), TASK-013 (new `InlineButtonSpinner` primitive + 8 inline button spinners in GrandRoundsMode + PatientEncounterMode), TASK-014 (widened to `InlineSpinner` with sm/md/lg/xl + 7 library-cluster spots across 6 files), TASK-015 (6 dashboard-cluster spots across 6 files), TASK-017 (7 analytics-cluster spots across 6 files), TASK-018 (15 drill + session migrations + 2 dead-import cleanups across 13 files). Running total: 35 migrations across 32 files, 2 dead-import cleanups, 4 intentional keeps. Remaining ~32 files across admin (~22), modes (~5), toolkit/misc (~5). Admin is the largest remaining cluster and should split into 2–3 sub-sprints. TASK-019+. |
| Auxiliary AI placeholders (Spark instant-calc, Smart Scribe infographic) | Per audit §10: defer unless roadmap activates. |
| Zod `.test.ts` fixtures flagged by improved audit (`authors/submit-question.test.ts`, `questions/attempt.test.ts`, `osce/analysis/grade.test.ts`, `osce/complete.test.ts`) | These are test harness fixtures, not production endpoints. |
| Cron endpoints without Zod (cron/batch-generate-questions, cron/compute-item-metrics, cron/content-quality-loop, cron/generate-daily-insights, cron/generate-variants, cron/push-reminders, cron/reservoir-maintenance) | Cron endpoints are gated by `CRON_SECRET` header, not user input. Adding request-body schemas adds little value; defer until the cron-auth model is reviewed as a unit. |
| Branch-merge endpoint (`branches/[branchName]/merge`) | **Already wrapper-validated** (`adminEndpoint(BranchMergeSchema, ...)` with rate limiting, audit logging, `safePrismaDisconnect`). Row is stale from the original script's false-positive sweep — PASSes the improved audit. No action needed. (podcast/generate closed in TASK-009.) |
| WARN (manual-only) endpoints — full §5 tail **closed** | Post-TASK-011 state: `knowledge/upload` + `technique-check/analyze` hardened with 415/413 gates and query-length bound. `sentry-tunnel` documented as intentional permanent steady state (Sentry envelope format is structurally incompatible with Zod). Improved audit on 2026-04-16 confirms only these three endpoints remain in `WARN_MANUAL_ONLY`, and all three are now correct-by-design. `questions/staging/[id]/check` and `webhooks/clerk` are no longer in `WARN_MANUAL_ONLY` under the improved audit — `webhooks/clerk` is correctly classified `WARN_OUT_OF_BAND` (Svix signature validation). |

## Status legend

- `pending` — queued, not started
- `in_progress` — currently being worked
- `completed` — implementation + verification done
- `partial` — partially completed; follow-up noted
- `obsolete` — no longer applicable after re-inspection
- `deferred` — moved out of this run

## Live status

| ID | Status | Commit | Notes |
|---|---|---|---|
| TASK-001 | completed | 0e0fed16 | OSCE `syncManager.queueAnswer` write removed; `updateConditionSchedule` preserved. |
| TASK-002 | obsolete | — | All admin/staging endpoints already wrapper-validated; script gap produced the false positive. |
| TASK-003 | obsolete | — | All admin/content endpoints already wrapper-validated; script gap produced the false positive. |
| TASK-004 | obsolete | — | All listed admin endpoints already wrapper-validated; script gap produced the false positive. |
| TASK-005 | obsolete | — | Split into TASK-007 + TASK-008. `performance/record` already wrapper-validated (generic-call form). `drill/log-attempt` is a 410 Gone tombstone. |
| TASK-006 | obsolete | — | All question-pipeline writes already wrapper-validated; script gap produced the false positive. |
| TASK-007 | completed | (pending commit) | `users/me/daily-plan.ts` POST switched to `authenticatedEndpoint(DailyPlanCompleteSchema, handler, { requestsPerMinute: 30 })`. GET unchanged. |
| TASK-008 | completed | (pending commit) | `users/me/exam-outcome.ts` POST rewritten to `authenticatedEndpoint(ExamOutcomeSchema, handler, { requestsPerMinute: 30 })` with enum/date/range bounds. |
| TASK-009 | completed | (pending commit) | `podcast/generate.ts` now content-type gated (415 for non-JSON/non-multipart), JSON branch runs `PodcastGenerateJsonSchema.safeParse()` with `.passthrough()`, multipart branch 413s above 25 MB. Audit: 1 FAIL remaining (log-attempt tombstone, expected). |
| TASK-010 | completed | `fbad2689` (code) / `6771368a` (docs) | `functions/api/questions/review.ts` tombstoned (both GET and POST → 410 Gone with migration pointers). Deleted `lib/services/review/reviewSubmissionService.ts`, `lib/services/review/reviewService.ts`, `lib/services/review/reviewService.test.ts`, and the now-empty `lib/services/review/` directory. Caller inventory was clean — no UI/hook/store importers. Audit: 2 FAILs, both tombstones, expected. |
| TASK-011 | completed | `acb0a0d5` (code) / `a15cfdd0` (docs) | `functions/api/knowledge/upload.ts` + `functions/api/technique-check/analyze.ts` hardened with 415 content-type gates, 413 `Content-Length` short-circuits, and proper HTTP-semantic rejections (oversized now 413 not 400). `technique-check/analyze.ts` also bounded `query` to `MAX_QUERY_CHARS = 2000`. `functions/api/sentry-tunnel.ts` left unchanged and documented as intentional `WARN_MANUAL_ONLY` steady state (Sentry envelope protocol is structurally incompatible with Zod; DSN + project-ID whitelist + IP rate-limit IS the right shape). Audit: 176/8/3/2 unchanged — hardening tightens manual validation within the WARN bucket without moving endpoints between buckets. |
| TASK-012 | completed | `e11b0457` (code) / `3b15f3f7` (docs) | Migrated 4 full-page loading blocks to canonical `DrillLoadingState`: `CoreAdaptiveSession.tsx` (`isLoading` return + Suspense fallback), `CramMode.tsx` (`!currentQuestion` guard), `GrandRoundsMode.tsx` (`viewState === 'loading'` + `!currentQuestion` guard). CramMode's real-progress generation block kept on `Loader2` (canonical skeleton can't represent `{completed}/{total}` counts). Inline button spinners (3 in GrandRoundsMode, 5 in PatientEncounterMode) deferred pending future `InlineButtonSpinner` primitive. Typecheck: 0 errors on touched files; pre-existing 1152 repo-wide errors unchanged. |
| TASK-013 | completed | `050a09df` (code) / `2dfb82d1` (docs) | New canonical `InlineButtonSpinner` primitive added to `components/loading/index.tsx` (SVG-based, `currentColor`-inheriting, `sm`/`md` sizes, `aria-hidden` by default). Migrated 3 inline button spinners in `GrandRoundsMode.tsx` (lines 702, 1027, 1194) and 5 in `PatientEncounterMode.tsx` (lines 1706, 2616 as `md`; 2722, 2775, 2793 as `sm`). `Loader2` import removed from GrandRoundsMode (now orphaned). Typecheck: 0 errors on touched files; pre-existing 1152 repo-wide errors unchanged. Audit: 176/8/3/2 unchanged (frontend-only change). |
| TASK-014 | completed | `0cb099fc` (code — bundled with calculator sprint-9) / `0b90329a` (docs) | Widened TASK-013 `InlineButtonSpinner` → canonical `InlineSpinner` in `components/loading/index.tsx` (now sm=w-4 / md=w-5 / lg=w-8 / xl=w-12; backward-compat alias + type re-exports preserve TASK-013 call sites). Migrated 7 library-cluster animate-spin spots across 6 files: `DrugReferenceLibrary.tsx` (xl overlay), `SmartPDFViewer.tsx` (lg overlay), `ContextWidget.tsx` (lg overlay), `ReferenceHub.tsx` ×2 (sm inline status rows), `HighYieldSummary.tsx` (sm inline counter spinner), `GenericReferenceView.tsx` (md container loader). Two `RefreshCw` refresh-button icons kept (non-loading semantic). Added `role="status"` / `aria-live="polite"` affordances on 5 spots that previously had none. Typecheck: 0 errors on touched files; repo-wide error count dropped from 1152 → 1151 (one orphaned `Loader2` cleanup). Audit: 176/8/3/2 unchanged (frontend-only change). |
| TASK-015 | completed | `9c2ea0b3` (code — combined with TASK-017) / `bda6752a` (docs) | Migrated 6 dashboard-cluster `animate-spin` loaders to canonical `InlineSpinner`: `InsightsHub.tsx` (lg), `RecommendationFeed.tsx` (lg), `GapAnalysisDashboard.tsx` (lg), `StudyPathDashboard/index.tsx` (lg), `StudyPathDashboard/ProgressProjectionChart.tsx` (lg), `ClinicalProfile/ClinicalProfileDashboard.tsx` (lg). One `RefreshCw` refresh-button icon in `RecommendationFeed.tsx:291` intentionally kept. All migrations pair with `role="status"` / `aria-live="polite"` on parents. Audit: 176/8/3/2 unchanged (frontend-only change). |
| TASK-017 | completed | `9c2ea0b3` (code — combined with TASK-015) / `bda6752a` (docs) | Migrated 7 analytics-cluster `animate-spin` loaders across 6 files: `AdvancedLearningProfileDashboard.tsx` (xl), `AnalyticsDashboard.tsx` (sm), `ConfusionMatrix.tsx` ×2 (sm), `LearningProfileDashboard.tsx` (lg), `SyllabusDecompiler.tsx` (xl), `TopicMasteryBreakdown.tsx` (md). One `RefreshCw` refresh-button icon in `DatabaseAnalyticsDashboard.tsx:245` intentionally kept. All migrations pair with `role="status"` / `aria-live="polite"` on parents. Audit: 176/8/3/2 unchanged (frontend-only change). |
| TASK-018 | completed | `a6b5f2cc` + `64d93d33` + `0280b661` + `31f26d99` (code — split across 4 parallel-agent commits; `31f26d99` folded remaining 8 files into a sibling useReducedMotion a11y sprint) / `bda6752a` (docs) | Migrated 15 drill + session `animate-spin` loaders across 13 files; removed 2 dead `Loader2` imports. Drill files: `DrillSetup` (lg), `ConditionDrillSession` ×2 (md+lg), `RapidRecallDrill` ×2 (xl+sm), `ElaborationDrill` (lg), `ContrastiveDrillSession` (lg), `PhotoDrillCard` (xl), `SystemDrillSession` + `PharmacologyDrillSession` (dead-import removal). Session files: `SessionScopeSelector` ×2 (lg+lg), `QuizView` (md — ~22-line handrolled SVG simplified to 1-line `InlineSpinner`), `NormalLabsPanel` (md), `ClinicalQuickRefPanel` (md), `SrsFlashcardView` ×2 (lg+sm). Zero intentional keeps. All migrations add `role="status"` / `aria-live="polite"`. Audit: 176/8/3/2 unchanged (frontend-only change). |
