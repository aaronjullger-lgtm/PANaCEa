Open browser to localhost:3000

Act as a Mobile Web Specialist. 

**Your Mission:**
1.  **Viewport Constraints:** simulate a mobile viewport (375px wide).
2.  **Check Navigation:**
    * Does the hamburger menu open and close smoothly?
    * Are the tap targets (buttons) large enough (min 44px)?
3.  **Check Study Mode:**
    * Do the questions fit on the screen without scrolling?
    * Are the "Answer" buttons easily reachable with a thumb?
4.  **The "Squish" Test:**
    * Look for text overlapping or flex containers breaking when screen space is tight.

**Output:**
* Apply CSS fixes for padding/margin issues immediately.
* If a component is fundamentally unusable on mobile, hide it or flag it.