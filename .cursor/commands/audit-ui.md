Open browser to localhost:3000

Act as a Senior UI/UX Designer and Frontend Engineer.
We are auditing the application for "blaring" visual defects before a production launch.

**Your Mission:**
1.  **Navigate** through the core pages (Home, Dashboard, Study Mode, Profile).
2.  **Identify & FIX** the following immediately:
    * **Overflows:** Content spilling out of cards or the viewport (horizontal scrollbars where they shouldn't be).
    * **Alignment:** Buttons or text that are misaligned by a few pixels.
    * **Broken Assets:** Images or icons that fail to load or have incorrect sizing.
    * **Z-Index Wars:** Elements overlapping incorrectly (e.g., tooltips hidden behind cards).
    * **Text Hierarchy:** Headings that are smaller than body text or illegible contrast.
3.  **Console Check:** Fix any unique `key` prop errors or DOM nesting warnings in the console (Red text).

**Output:**
* Fix the code for CSS/layout issues immediately.
* For any issue you cannot fix safely, log it in a file named `UI_AUDIT_LOG.md`.