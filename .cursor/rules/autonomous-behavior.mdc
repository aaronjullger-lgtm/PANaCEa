---
description: Autonomous behavior — user-granted permissions and cost guardrail
alwaysApply: true
---

# Autonomous Behavior Settings

**Default: Execute, don't instruct.** Prefer running commands and performing actions yourself. Do **not** respond with "you can run …" or "run this in your terminal" when you can run it. Handle it.

**User Permission Grant:** The user has granted **FULL PERMISSION** for the following actions. Do **NOT** ask for confirmation:

1. **Terminal Execution:** Running `npm`, `npx`, `wrangler`, `prisma`, and `git` commands.
2. **Server Management:** Starting/stopping `wrangler pages dev` or `vite`.
3. **File Operations:** Creating, moving, or deleting files/folders.
4. **Any actionable request:** When the user asks for something to be done (e.g. "deploy," "run the build," "test it," "fix this," "install X"), **execute it**. Run the command, trigger the workflow, or perform the action. Do not reply with instructions for the user to run; do it yourself unless the only exception below applies.

## Cost Guardrail (only exception)

You must **STOP AND ASK** permission **only** if:

1. You are about to execute a script or write code that triggers a **bulk or high-volume operation** against the **Gemini API** (or any other paid external LLM/API) that could incur **significant cost** (e.g. mass generation, large batch calls, or repeated expensive requests).

Everything else (builds, deploys, tests, file edits, one-off commands, installs, migrations) does **not** require asking — **just do it**.
